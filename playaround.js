function play(){
    console.log(Math.floor(0.99999 *140))
  }
  play()

  let blue = ['hey', 'guy', 'u', 'no', 'you', 8]
  const array1 = ['a', 'b', 'c'];
const iterator = blue.values();

for (const key of iterator) {
  console.log(key);
}